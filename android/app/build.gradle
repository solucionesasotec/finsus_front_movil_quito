// plugins {
//     id "com.android.application"
//     id "kotlin-android"
//     // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
//     id "dev.flutter.flutter-gradle-plugin"
// }


// def keystoreProperties = new Properties()
// def keystorePropertiesFile = rootProject.file("key.properties")
// if (keystorePropertiesFile.exists()) {
//     keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
// }


// def localProperties = new Properties()
// def localPropertiesFile = rootProject.file("local.properties")
// if (localPropertiesFile.exists()) {
//     localPropertiesFile.withReader("UTF-8") { reader ->
//         localProperties.load(reader)
//     }
// }

// def flutterVersionCode = localProperties.getProperty("flutter.versionCode")
// if (flutterVersionCode == null) {
//     flutterVersionCode = "1"
// }

// def flutterVersionName = localProperties.getProperty("flutter.versionName")
// if (flutterVersionName == null) {
//     flutterVersionName = "1.0"
// }

// android {
//     namespace = "ec.finsus.bancamovilquito"
//     compileSdk = flutter.compileSdkVersion
//     ndkVersion = flutter.ndkVersion

//     compileOptions {
//         sourceCompatibility = JavaVersion.VERSION_1_8
//         targetCompatibility = JavaVersion.VERSION_1_8
//     }

//     defaultConfig {
//         // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
//         applicationId = "ec.finsus.bancamovilquito"
//         // You can update the following values to match your application needs.
//         // For more information, see: https://docs.flutter.dev/deployment/android#reviewing-the-gradle-build-configuration.
//         minSdk = flutter.minSdkVersion
//         targetSdk = flutter.targetSdkVersion
//         versionCode = flutterVersionCode.toInteger()
//         versionName = flutterVersionName
//     }
//         // Cargar las propiedades del archivo key.properties
//     def keystoreProperties = new Properties()
//     def keystorePropertiesFile = rootProject.file("key.properties")
//     if (keystorePropertiesFile.exists()) {
//         keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
//     }

//     signingConfigs {
//         release {
//             keyAlias keystoreProperties['keyAlias']
//             keyPassword keystoreProperties['keyPassword']
//             storeFile file(keystoreProperties['storeFile'])
//             storePassword keystoreProperties['storePassword']
//         }
//     }
//     buildTypes {
//         release {
//             // TODO: Add your own signing config for the release build.
//             // Signing with the debug keys for now, so `flutter run --release` works.
//             signingConfig = signingConfigs.debug
//              minifyEnabled false // Cambia a true si usas herramientas de optimización como ProGuard
//             shrinkResources false // Cambia a true si deseas eliminar recursos no utilizados
//         }
//     }
// }

// // android {
// //     namespace = "ec.finsus.bancamovilquito"
// //     compileSdk = flutter.compileSdkVersion
// //     ndkVersion = flutter.ndkVersion

// //     compileOptions {
// //         sourceCompatibility = JavaVersion.VERSION_1_8
// //         targetCompatibility = JavaVersion.VERSION_1_8
// //     }

// //     defaultConfig {
// //         applicationId = "ec.finsus.bancamovilquito"
// //         minSdk = flutter.minSdkVersion
// //         targetSdk = flutter.targetSdkVersion
// //         versionCode = flutterVersionCode.toInteger()
// //         versionName = flutterVersionName
// //     }

// //     // Cargar las propiedades del archivo key.properties
// //     def keystoreProperties = new Properties()
// //     def keystorePropertiesFile = rootProject.file("key.properties")
// //     if (keystorePropertiesFile.exists()) {
// //         keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
// //     }

// //     signingConfigs {
// //         release {
// //             keyAlias keystoreProperties['keyAlias']
// //             keyPassword keystoreProperties['keyPassword']
// //             storeFile file(keystoreProperties['storeFile'])
// //             storePassword keystoreProperties['storePassword']
// //         }
// //     }

// //     buildTypes {
// //         release {
// //             signingConfig signingConfigs.release
// //             minifyEnabled false // Cambia a true si usas herramientas de optimización como ProGuard
// //             shrinkResources false // Cambia a true si deseas eliminar recursos no utilizados
// //         }
// //     }
// // }

// flutter {
//     source = "../.."
// }
plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

// Cargar las propiedades del archivo key.properties (esto solo debe estar una vez, al principio)
def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file("key.properties")
print ("Ubicacion " + keystorePropertiesFile)
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}
else {
   // print ("error"+keystorePropertiesFile)++++++++++++ 
    throw new GradleException("El archivo key.properties no se encontró.")
}

def localProperties = new Properties()
def localPropertiesFile = rootProject.file("local.properties")
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader("UTF-8") { reader ->
        localProperties.load(reader)
    }
}

def flutterVersionCode = localProperties.getProperty("flutter.versionCode")
if (flutterVersionCode == null) {
    flutterVersionCode = "1"
}

def flutterVersionName = localProperties.getProperty("flutter.versionName")
if (flutterVersionName == null) {
    flutterVersionName = "1.0"
}

android {
    namespace = "ec.finsus.bancamovilquito"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    
    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "ec.finsus.bancamovilquito"
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutterVersionCode.toInteger()
        versionName = flutterVersionName
    }

    signingConfigs {
        release {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile file(keystoreProperties['storeFile'])
            storePassword keystoreProperties['storePassword']
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false // Cambia a true si usas herramientas de optimización como ProGuard
            shrinkResources false // Cambia a true si deseas eliminar recursos no utilizados
        }
    }
}

flutter {
    source = "../.."
}

subprojects {
    afterEvaluate { project ->
        if (project.hasProperty('android')) {
            // Forzar namespace si falta
            if (project.android.namespace == null) {
                if (project.name == "downloads_path_provider_28") {
                    project.android.namespace = "it.nplace.downloadspathprovider"
                } else {
                    project.android.namespace = project.group
                }
            }
            
            // CONFIGURACIÓN PARA 16 KB (Requisito de Google Play)
            project.android {
                packagingOptions {
                    jniLibs {
                        useLegacyPackaging = false
                    }
                }
            }
        }
    }
}